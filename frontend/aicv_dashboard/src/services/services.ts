/**
 * Legacy Services Export
 * For backward compatibility with existing code
 */

import { authStorage } from './storage';

// Export with old name for backward compatibility
export const authTokens = authStorage;

